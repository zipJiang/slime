# Frozen robust LocBench harness

This immutable source snapshot was exported from rollout-controller commit `be3d85ace36f10f3b4ae71804ec2c3929ed7ce68` on branch `workspace-harness-robust`. `source-manifest.json` authenticates every runtime file. The empty `examples/__init__.py` prevents another snapshot from claiming the namespace package.
